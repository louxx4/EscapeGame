<Window x:Class="EscapeGame.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EscapeGame"
        xmlns:vm="clr-namespace:EscapeGame.ViewModels"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        mc:Ignorable="d"
        Height="450" Width="800" 
        WindowStyle="None" ResizeMode="NoResize" WindowState="{Binding PWindowState}">
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <materialDesign:DrawerHost Grid.RowSpan="3" HorizontalAlignment="Left" MinWidth="200" 
                                   IsLeftDrawerOpen="{Binding PIsMenuOpen, Mode=TwoWay}">
            <materialDesign:DrawerHost.LeftDrawerContent>
                <DockPanel MinWidth="200" Background="{StaticResource MaterialDesignColumnHeader}">
                </DockPanel>
            </materialDesign:DrawerHost.LeftDrawerContent>
        </materialDesign:DrawerHost>
        <DockPanel VerticalAlignment="Bottom" Grid.Row="3" Grid.ColumnSpan="3">
            <materialDesign:ColorZone
                    Padding="5"
                    materialDesign:ShadowAssist.ShadowDepth="Depth2"
                    Mode="PrimaryDark"
                    DockPanel.Dock="Left"
                    MouseDown="ColorZone_MouseDown">
                <DockPanel>
                    <ToggleButton
                                x:Name="MenuToggleButton"
                                Style="{StaticResource MaterialDesignHamburgerToggleButton}"
                                IsChecked="{Binding PIsMenuOpen}"
                                AutomationProperties.Name="HamburgerToggleButton"
                                ToolTip="Show backpack"/>
                    <Button
                        DockPanel.Dock="Right"
                        HorizontalAlignment="Right"
                        Style="{StaticResource MaterialDesignIconForegroundButton}"
                        Command="{Binding CmdClose}"
                        ToolTip="Escape the escape game. Cheater.">
                        <materialDesign:PackIcon Kind="WindowClose" Width="35" Height="35"/>
                    </Button>
                    <ToggleButton
                        DockPanel.Dock="Right"
                        HorizontalAlignment="Right"
                        Style="{StaticResource MaterialDesignIconForegroundButton}"
                        IsChecked="{Binding PIsMaximized, Mode=TwoWay}"
                        ToolTip="Maximize window">
                        <materialDesign:PackIcon Kind="WindowMaximize" Width="30" Height="30"/>
                    </ToggleButton>
                    <Button
                        DockPanel.Dock="Right"
                        HorizontalAlignment="Right"
                        Style="{StaticResource MaterialDesignIconForegroundButton}"
                        Command="{Binding CmdMinimize}"
                        ToolTip="Minimize window">
                        <materialDesign:PackIcon Kind="WindowMinimize"/>
                    </Button>
                </DockPanel>
            </materialDesign:ColorZone>
        </DockPanel>
    </Grid>
</Window>
